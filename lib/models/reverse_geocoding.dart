class ReverseGeocodingPlace {
  int id;
  double distanceWithinMeters;
  String address;
  String area;
  String city;
  ReverseGeocodingPlace(
      {this.id, this.distanceWithinMeters, this.address, this.area, this.city});
}
